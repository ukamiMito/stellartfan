name: Fetch Videos (Polling)

on:
  schedule:
    # 10分間隔で uploads を再スキャンし、live/ended を更新
    - cron: '*/10 * * * *'
  workflow_dispatch:

concurrency:
  group: youtube-data-update
  cancel-in-progress: true

jobs:
  fetch-videos:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install node-fetch@3

      - name: Run fetch_videos.js
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: node scripts/fetch_videos.js

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add docs/assets/data/videos.json
          git commit -m "Update videos.json" || exit 0
          git push
